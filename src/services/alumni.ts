import { Alumni } from '@/types/alumni';

export const alumniService = {
  getAlumni: () => {
    return [];
  },
  
  getAlumniById: (id: string) => {
    return undefined;
  },
  
  searchAlumni: (query: string) => {
    return [];
  }
};
